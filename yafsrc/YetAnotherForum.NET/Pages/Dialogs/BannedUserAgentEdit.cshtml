@using Microsoft.AspNetCore.Mvc.TagHelpers
@using YAF.Core.Helpers
@using YAF.Core.Utilities
@using YAF.Web.HtmlHelpers
@using YAF.Web.TagHelpers

@model YAF.Types.Modals.BannedEmailEditModal

<!-- Modal -->
<div class="modal fade" id="EditDialog" tabindex="-1" role="dialog" aria-labelledby="EditDialog" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">
                    @Html.LocalizedText("ADMIN_BANNED_USERAGENTS_EDIT", Model.Id.HasValue ? "TITLE_EDIT" : "TITLE")
                </h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close">
                </button>
            </div>
            <div class="modal-body">
                <form asp-controller="BannedUserAgent" asp-action="Edit" class="needs-validation">
                    <!-- Modal Content START !-->
                    <input asp-for="Id" type="hidden" value="@Model.Id" />
                    <div class="mb-3">
                        <label info-button="true" class="form-label"
                               asp-for="Mask"
                               localized-tag="MASK" localized-page="ADMIN_BANNED_USERAGENTS_EDIT"></label>
                        <input asp-for="Mask" class="form-control" required="required" type="text"/>
                        <div class="invalid-feedback">
                            @Html.LocalizedText("ADMIN_BANNED_USERAGENTS_EDIT","NEED_MASK")
                        </div>
                    </div>
                    <!-- Modal Content END !-->
                </form>
            </div>
            <div class="modal-footer">
                <button button-style="Primary" data-bs-save="editModal"
                        text-localized-tag="@(Model.Id.HasValue ? "SAVE": "ADD_IP")"
                        text-localized-page="ADMIN_BANNED_USERAGENTS"
                        Icon="save">
                </button>
                <button type="button" button-style="Secondary" icon="times" data-bs-dismiss="modal" text-localized-tag="CANCEL"></button>
            </div>
        </div>
    </div>
</div>

<script>
     @Html.Raw(JsAndCssHelper.CompressJavaScript(JavaScriptBlocks.ModalNotifyJs()));

     @Html.Raw(JsAndCssHelper.CompressJavaScript(JavaScriptBlocks.FormValidatorJQueryJs()));
     
     @Html.Raw(JsAndCssHelper.CompressJavaScript(JavaScriptBlocks.ToolTipJs()));
</script>